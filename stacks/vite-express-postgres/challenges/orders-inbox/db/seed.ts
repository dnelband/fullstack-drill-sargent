import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for orders-inbox
  void pool;
  console.log("[db] orders-inbox seed placeholder");
}
