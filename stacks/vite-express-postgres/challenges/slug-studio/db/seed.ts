import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for slug-studio
  void pool;
  console.log("[db] slug-studio seed placeholder");
}
