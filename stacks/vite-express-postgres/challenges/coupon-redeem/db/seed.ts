import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for coupon-redeem
  void pool;
  console.log("[db] coupon-redeem seed placeholder");
}
