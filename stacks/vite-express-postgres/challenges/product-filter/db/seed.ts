import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for product-filter
  void pool;
  console.log("[db] product-filter seed placeholder");
}
