import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for ticket-claim
  void pool;
  console.log("[db] ticket-claim seed placeholder");
}
