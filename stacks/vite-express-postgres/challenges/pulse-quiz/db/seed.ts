import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for pulse-quiz
  void pool;
  console.log("[db] pulse-quiz seed placeholder");
}
