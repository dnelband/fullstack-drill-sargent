import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for brief-desk
  void pool;
  console.log("[db] brief-desk seed placeholder");
}
