import type { Pool } from "pg";

export async function seedChallenge(pool: Pool): Promise<void> {
  // TODO: CREATE TABLE + seed data for leave-desk
  void pool;
  console.log("[db] leave-desk seed placeholder");
}
