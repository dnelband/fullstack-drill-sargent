// Implement Seat Hold (see challenge README).
// CURRENT_MEMBER_ID and SEAT_HOLD_TTL_MS are in shared/seat-hold ("m1", 60000).

export function ChallengeApp() {
  return (
    <div className="min-h-screen bg-slate-950 p-6 text-slate-100">
      <div className="mx-auto max-w-3xl rounded-2xl border border-slate-800 bg-slate-900/70 p-6">
        <h1 className="text-3xl font-semibold">Seat Hold</h1>
        <p className="mt-2 text-slate-400">Implement the seat hold UI here.</p>
      </div>
    </div>
  );
}
